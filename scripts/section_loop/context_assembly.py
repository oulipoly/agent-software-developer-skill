"""Compatibility wrapper for context sidecar resolution."""

from lib.context_sidecar import (
    VALID_CATEGORIES,
    _RESOLVERS,
    _resolve_allowed_tasks,
    _resolve_codemap,
    _resolve_coordination_state,
    _resolve_decision_history,
    _resolve_flow_context,
    _resolve_model_policy,
    _resolve_related_files,
    _resolve_section_output,
    _resolve_section_spec,
    _resolve_strategic_state,
    materialize_context_sidecar,
    parse_context_field,
    resolve_context,
)
