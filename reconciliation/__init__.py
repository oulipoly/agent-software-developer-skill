"""Reconciliation system: output validation and conflict resolution.

Public API (import from submodules):
    cross_section_reconciler: load_reconciliation_result
    reconciliation_phase: ReconciliationPhaseExit, run_reconciliation_phase
    queue (repository): queue_reconciliation_request
"""
